# UniPay Contract Tests

This package is intentionally separate from the future production workspace. It
checks static contract assets before Gateway code exists and can later be added
as a workspace member.

Run:

```text
cargo test --manifest-path tests/contract-tests/Cargo.toml
```
